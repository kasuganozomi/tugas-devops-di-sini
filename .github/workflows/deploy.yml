name: Clean Self-Hosted Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: 1. Ambil Kode Repositori (Checkout)
        uses: actions/checkout@v4

      - name: 2. Instalasi Dependensi & Kompilasi
        run: |
          bun install
          bun run build

      - name: 3. Sinkronisasi Bersih ke Direktori Produksi (/app/tugas-devops)
        run: |
          mkdir -p /app/tugas-devops
          rsync -av --delete --exclude='.git' --exclude='.github' --exclude='node_modules' ./ /app/tugas-devops/
          echo "Deployment berhasil disinkronkan ke /app/tugas-devops!"
